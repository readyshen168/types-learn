{
    "compilerOptions": {
        "paths": { "*": ["./src/*"] },

        "outDir": "dist",
        "noEmitOnError": true,
        "target": "es5",

        // 根据目标运行环境选择
        "lib": ["ES2015", "DOM"]
    }
}